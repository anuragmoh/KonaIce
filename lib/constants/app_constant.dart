
enum FinixTagsKey {
  customerEmail,
  customerName,
  eventName,
  eventCode,
  environment,
  paymentMethod,
  username,
  password,
  application,
  version,
  merchantId,
  deviceID,
  amount,
  serialNumber,
  tags
}

enum CardDetails { cardNumber, expirationMonth, expirationYear, zipcode, cvv }